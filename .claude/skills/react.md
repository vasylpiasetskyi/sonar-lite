# Skill Notes: React

- Functional components + hooks only.
- Colocate a component with its styles and minimal local state.
- Use React Query for server state — don't hand-roll `useEffect` fetch/cache logic.
- Use React Hook Form for forms.
- Use Recharts for trend charts.
