# Rule: Coding Style

- Keep functions under ~40 lines.
- Prefer meaningful names over comments.
- Type hints on every Python function; explicit types in TypeScript, no `any`.
- Comments only explain non-obvious "why", never "what".
